program DeclarationTest;

// Test various types of declarations
var simpleInt : Integer;
var simpleReal : Real;
var simpleBool : Boolean;

// Test multiple variables per line
var x, y, z : Integer;
var a, b : Real;

// Test Array declarations
var intArray : Array[1..10] of Integer;
var realArray : Array[0..5] of Real;
var boolArray : Array[-5..-1] of Boolean;

begin
    // A minimal body is needed for the parser to accept the program.
    x := 1
end